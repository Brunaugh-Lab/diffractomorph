# Synthetic minimal project

This example contains invented detector data for an arbitrary compound named
“compound X.” It deliberately uses four named channels and the vendor-neutral CSV
adapter. It is not calibrated to particle size or mass and contains no CFZ, R3,
31-channel, institutional, or private-path assumptions.

The project manifest is `project.yaml` in this directory.
